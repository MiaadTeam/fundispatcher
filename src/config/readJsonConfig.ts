import { ServicesConfig } from "./../types/mod.ts";

/**
 * @todo it should have return type
 * @param path
 */
export const readJsonConfig = (
  path: string = "./serverConfig.json"
): ServicesConfig => {};
